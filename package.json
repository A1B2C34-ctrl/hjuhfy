{
  "name": "shaw-webmail",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hugo --gc --minify",
    "dev": "hugo server -D",           // Start Hugo's local server with drafts enabled
    "start": "serve -s public -l ${PORT:-3000}" // Serve the built site from public/
  },
  "dependencies": {
    "serve": "^14.0.1"
  },
  "devDependencies": {
    "hugo-bin": "^0.87.0"
  }
}
